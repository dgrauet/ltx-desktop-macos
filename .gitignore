# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
*.egg-info/
dist/
build/DerivedData/
python-embed/
python-deps-hash.txt
backend/requirements-dist.txt
release/

# Model / LoRA weights (large — never commit; they live in ~/.ltx-desktop or are
# downloaded separately). Applies anywhere in the tree, not just models/.
*.safetensors
*.gguf
*.ckpt
*.mlx
*.pt
*.pth
models/*.bin

# LoRA library / training data if ever copied into the repo
**/loras/
**/datasets/
**/.precomputed/

# Xcode
app/LTXDesktop.xcodeproj/project.xcworkspace/xcuserdata/
app/LTXDesktop.xcodeproj/xcuserdata/
DerivedData/
*.xcuserstate

# IDE
.vscode/
.idea/

# Environment
.env
.env.local
